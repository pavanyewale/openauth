import 'package:flutter/material.dart';

class BargraphDashboardWidget extends StatelessWidget {
  const BargraphDashboardWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
